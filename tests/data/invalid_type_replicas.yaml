apiVersion: apps/v1
kind: Deployment
metadata:
  name: bad-replicas
spec:
  replicas: "three"
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: nginx
